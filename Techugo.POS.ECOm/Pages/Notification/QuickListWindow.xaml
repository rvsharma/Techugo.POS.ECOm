<Window x:Class="Techugo.POS.ECOm.Pages.Notification.QuickListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quick List"
        Width="320"
        Height="420"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False">

    <!--
        Strategy:
        - Draw a slightly larger dark rounded "shadow" element behind the content
          and blur it with BlurEffect. Place the real white panel on top.
        - This avoids DropShadowEffect clipping issues and produces a uniform soft shadow.
    -->
    <Grid>
        <!-- Shadow layer: bigger than main panel and blurred -->
        <Border CornerRadius="14"
                Background="#000000"
                Opacity="0.12"
                Margin="6,8,6,6"   
                IsHitTestVisible="False">
            <Border.Effect>
                <BlurEffect Radius="18"/>
            </Border.Effect>
        </Border>

        <!-- Main white panel -->
        <Border CornerRadius="12"
                Background="White"
                Padding="12"
                BorderBrush="#E6E6E6"
                BorderThickness="1">
            <DockPanel Width="296" Height="396">
                <!-- Header -->
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                    <TextBlock Text="Notifications" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
                    <Border Background="#FF3B30" CornerRadius="10" Margin="8,0,0,0" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="1" Foreground="White" FontSize="12" FontWeight="Bold"/>
                    </Border>

                    <Button Content="✕"
                            Width="26"
                            Height="26"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            DockPanel.Dock="Right"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Foreground="#000000"/>
                </DockPanel>

                <!-- Separator -->
                <Border Height="1" Background="#EFEFF0" DockPanel.Dock="Top" Margin="0,0,0,8"/>

                <!-- Scrollable list -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" DockPanel.Dock="Top">
                    <ItemsControl ItemsSource="{Binding Notifications}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <!-- Notification card -->
                                <Border
                                        Background="#f3f8ff"
                                        BorderBrush="#e0e0e0"
                                        BorderThickness="1"
                                        CornerRadius="20"
                                        Margin="4"
                                        Padding="0"
                                        Height="100">
                                                                        <Border
                                            Background="#f3f8ff"
                                            BorderBrush="#2b7fff"
                                            BorderThickness="2,0,0,0"
                                            CornerRadius="20">
                                        <StackPanel Margin="4">

                                            <TextBlock Text="{Binding Title}" FontWeight="DemiBold" FontSize="14" Margin="0,8,0,0"/>
                                            <TextBlock Text="{Binding Message}" FontSize="12" Foreground="#888" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Footer actions -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" DockPanel.Dock="Bottom" Margin="0,8,0,0">
                    <Button Content="Mark All Read" Margin="0,0,8,0" Padding="12,6" Background="#FFF" BorderBrush="#E6E6E6" />
                    <Button Content="Clear All" Padding="12,6" Background="#FFF" BorderBrush="#E6E6E6" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
