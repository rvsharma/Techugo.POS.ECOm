<UserControl x:Class="Techugo.POS.ECOm.Pages.NewOrderPopUp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="400"
              Background="Transparent" ClipToBounds="False">
    <Border Background="White"
        CornerRadius="12"
        Padding="18"
        BorderBrush="#FF868686"
        BorderThickness=".3"
        MouseDown="PopupBorder_MouseDown">
        <Border.Effect>
            <DropShadowEffect Color="#33000000" BlurRadius="40" ShadowDepth="2" Opacity="0.45"/>
        </Border.Effect>
        <StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                <Grid Width="40" Height="40" VerticalAlignment="Center">
                    <Ellipse Fill="#E8FBF1" Width="36" Height="36"/>
                    <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" Foreground="#00a63e" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <Border Background="#00c951" Height="18" Width="Auto" Margin="10,4,0,0" CornerRadius="4" VerticalAlignment="Top">
                    
                    <TextBlock Text="New Order"  Foreground="White" FontWeight="DemiBold" FontSize="12" Padding="10,0"  VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="4:21:54 PM" Foreground="#6B6B6B" FontSize="12" Margin="10,4,0,0" VerticalAlignment="Top"/>
            </StackPanel>
            <TextBlock Text="ORD-2024-1237" FontWeight="Medium" FontSize="14" Foreground="#111111" Margin="50,0,0,0"/>
            <StackPanel Orientation="Horizontal" Margin="50,8,0,0">
                <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" Foreground="#6B6B6B" FontSize="15" VerticalAlignment="Center"/>
                <TextBlock Text="Jane Smith" FontSize="14" Foreground="#111111" Margin="6,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="50,4,0,0">
                <TextBlock Text="3 items" FontSize="12" Foreground="#6B6B6B"/>
                <TextBlock Text="?554.33" FontSize="12" Foreground="#00a63e" FontWeight="Bold" Margin="12,0,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="50,4,0,0">
                <TextBlock Text="&#xE707;" FontFamily="Segoe MDL2 Assets" Foreground="#6B6B6B" FontSize="14" VerticalAlignment="Center"/>
                <TextBlock Text="North Block" FontSize="12" Foreground="#6B6B6B" Margin="6,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
                <!-- Accept Order Button with rounded corners -->
                <Border CornerRadius="6" Background="#00a63e" Margin="0,0,12,0" BorderBrush="#00a63e" Height="32" BorderThickness="1">
                    <Button Background="Transparent" Foreground="White" FontWeight="SemiBold" FontSize="12" Padding="18,2" BorderThickness="0" Click="AcceptOrder_Click" Height="32">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal" Margin="12,0">
                            <TextBlock Text="&#xE8FB;" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="Accept Order"/>
                        </StackPanel>
                    </Button>
                </Border>
                <Border CornerRadius="6" Background="Transparent" Margin="0,0,12,0" BorderBrush="#E53935" Height="32" BorderThickness="1">
                    <Button Background="Transparent" Foreground="#E53935" FontWeight="SemiBold" FontSize="12" Padding="18,2" BorderThickness="0" Click="AcceptOrder_Click" Height="32">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal" Margin="12,0">
                            <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0,0,6,0"/>
                            <TextBlock Text="Reject Order"/>
                        </StackPanel>
                    </Button>
                </Border>
                
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
