<UserControl x:Class="Techugo.POS.ECOm.Pages.Dashboard.AssignRider"
                          xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:Techugo.POS.ECOm.Converters"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <conv:StatusToBackgroundConverter x:Key="StatusToBackgroundConverter" />
        <conv:StatusToForegroundConverter x:Key="StatusToForegroundConverter" />
    </UserControl.Resources>
    <Grid Background="#FCFAFB">
        <StackPanel Margin="16,24,16,0">
            <!-- Title Row: Back Button and All Orders side by side -->
            <Grid Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Back Button -->
                <Border Grid.Column="0" Width="Auto" Height="36" Padding="8,0,12,0" Background="#fff" BorderBrush="#e3e3e3" CornerRadius="8" BorderThickness="1" MouseLeftButtonUp="BackButton_Click"
TouchUp="BackButton_Click" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <svg:SvgViewbox Source="/Assets/Images/back.svg" Width="12" Height="12" VerticalAlignment="Center" Margin="8,0,8,0"/>
                        <TextBlock Text="Back to Dashboard" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <!-- Spacer -->

                <!-- All Orders Title -->
                <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="Assign Rider Orders" FontWeight="Bold" FontSize="24"/>
                    <TextBlock Text="Manage orders ready for rider assignment" FontSize="14" Foreground="#888" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
            <!-- Orders Card -->
            <Border Background="#fff" BorderBrush="#e3e3e3" BorderThickness="1" CornerRadius="20" Padding="24" Margin="0,24,0,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <svg:SvgViewbox Source="/Assets/Images/orders.svg" Width="18" Height="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Assign Rider Orders (0 orders)" FontSize="16" FontWeight="DemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <!-- Table Header -->
                    <ListView x:Name="OrdersListView" ItemsSource="{Binding orderData}" Margin="0,24,0,0" BorderThickness="0" SelectionMode="Single">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Margin" Value="0,0,0,2"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F3F8FF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Order ID" Width="120" DisplayMemberBinding="{Binding OrderNo}" />
                                <GridViewColumn Header="Customer" Width="120" DisplayMemberBinding="{Binding Customer.CustomerName}" />
                                <GridViewColumn Header="Contact" Width="120" DisplayMemberBinding="{Binding Customer.MobileNo}" />
                                <GridViewColumn Header="Address" Width="240">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Address}" TextWrapping="Wrap" Width="170"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Items" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ItemsControl ItemsSource="{Binding ItemImages}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Vertical"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Image Source="{Binding}" Width="36" Height="36" Margin="0,2,0,2"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Total Amount" Width="140" DisplayMemberBinding="{Binding TotalAmount}" />
                                <GridViewColumn Header="Payment" Width="120" DisplayMemberBinding="{Binding PaidAmount}" />
                                <GridViewColumn Header="Schedule" Width="120" DisplayMemberBinding="{Binding BranchDeliverySlot}" />
                                <GridViewColumn Header="Actions" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Assign Rider" Width="60" Height="28" Click="OpenOrderDetailPoPUp_Click"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                        <ListView.Resources>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="Height" Value="48"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GridViewColumnHeader">
                                            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.Resources>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
        <materialDesign:Snackbar x:Name="SuccessSnackbar"
                          MessageQueue="{materialDesign:MessageQueue}" 
                          VerticalAlignment="Bottom"
                          HorizontalAlignment="Right"
                          Margin="0,0,24,24"/>
    </Grid>
</UserControl>