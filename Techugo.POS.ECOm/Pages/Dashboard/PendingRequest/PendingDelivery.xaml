<UserControl x:Class="Techugo.POS.ECOm.Pages.Dashboard.PendingDelivery"
    xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="Auto" Width="Auto">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Background="#FCFAFB">
        <StackPanel Margin="16,24,16,0">
            <!-- Title Row: Back Button and All Orders side by side -->
            <Grid Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Back Button -->
                <Border Grid.Column="0" Width="Auto" Height="36" Padding="8,0,12,0" Background="#fff" BorderBrush="#e3e3e3" CornerRadius="8" BorderThickness="1" MouseLeftButtonUp="BackButton_Click"
TouchUp="BackButton_Click" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <svg:SvgViewbox Source="/Assets/Images/back.svg" Width="12" Height="12" VerticalAlignment="Center" Margin="8,0,8,0"/>
                        <TextBlock Text="Back to Dashboard" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <!-- Spacer -->

                <!-- All Orders Title -->
                <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="Pending Delivery Orders" FontWeight="Bold" FontSize="24"/>
                    <TextBlock Text="Review and manage pending delivery requests" FontSize="14" Foreground="#888" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
            <!-- Orders Card -->
            <Border Background="#fff" BorderBrush="#e3e3e3" BorderThickness="1" CornerRadius="20" Padding="24" Margin="0,24,0,0">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,16">
                            <svg:SvgViewbox Source="/Assets/Images/orders.svg" Width="18" Height="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding TotalOrdersText}" FontSize="16" FontWeight="DemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding SelectedCount, StringFormat={}{0} selected}" VerticalAlignment="Center"
                                Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}" Foreground="#666" FontSize="12" />

                            <StackPanel Orientation="Horizontal" Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
                                <Button Click="AcceptSelected_Click" Height="36" Padding="12,0" Margin="8,0,8,0" Foreground="White" FontWeight="SemiBold" Style="{StaticResource RoundedGreenButtonStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <svg:SvgViewbox Source="/Assets/Images/ready.svg" Width="14" Height="14" Margin="8" VerticalAlignment="Center"/>
                                        <TextBlock Text="Accept Selected" Margin="6,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Click="RejectSelected_Click" Height="36" Padding="12,0" Foreground="White" FontWeight="SemiBold" Style="{StaticResource RoundedRedButtonStyle}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <svg:SvgViewbox Source="/Assets/Images/circle_cross.svg" Width="14" Height="14" Margin="8" VerticalAlignment="Center"/>
                                        <TextBlock Text="Reject Selected" Margin="6,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    <!-- Table Header -->
                    <ListView x:Name="OrdersListView" ItemsSource="{Binding orderData}" Margin="0,24,0,0" BorderThickness="0" SelectionMode="Extended">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Margin" Value="0,0,0,2"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="MinHeight" Value="64"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#0a0a0a"/>
                                <Setter Property="FontSize" Value="14"/>

                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#ececf0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <!-- Select column with header checkbox -->
                                <GridViewColumn Width="40">
                                    <GridViewColumn.Header>
                                        <CheckBox x:Name="SelectAllCheckBox" 
                                                  Style="{StaticResource MaterialDesignCheckBox}"
                                                  Click="SelectAllCheckBox_Click" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="7" />
                                    </GridViewColumn.Header>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                                      Style="{StaticResource MaterialDesignCheckBox}"
                                                      VerticalAlignment="Center" HorizontalAlignment="Center" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Header="Order ID" Width="120" DisplayMemberBinding="{Binding Item.OrderNo}" />
                                <GridViewColumn Header="Customer" Width="120" DisplayMemberBinding="{Binding Item.Customer.CustomerName}" />
                                <GridViewColumn Header="Contact" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock>
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="+91 {0:00000 00000}">
                                                        <Binding Path="Item.Customer.MobileNo"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Header="Address" Width="220">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Item.ShortAddress}" TextWrapping="Wrap" Width="170"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Header="Items" Width="80" DisplayMemberBinding="{Binding Item.Items}" />
                                <GridViewColumn Header="Total Amount" Width="100" DisplayMemberBinding="{Binding Item.TotalAmount, StringFormat=₹{0}}" />
                                <GridViewColumn Header="Payment" Width="80" DisplayMemberBinding="{Binding Item.PaymentMode}" />
                                <GridViewColumn Header="Schedule" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Item.ExpectedDeliveryDate, StringFormat='{}{0:yyyy-MM-dd, hh:mm tt}'}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Header="Order Type" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border x:Name="Badge" CornerRadius="8" Padding="8,4" Background="#F3F4F6" HorizontalAlignment="Left" Margin="0,6,0,6">
                                                <TextBlock x:Name="BadgeText" Text="{Binding Item.OrderType}" Foreground="#111827" FontWeight="SemiBold" FontSize="12" />
                                            </Border>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding Item.OrderType}" Value="Subscription Order">
                                                    <Setter TargetName="Badge" Property="Background" Value="#F5E6FF"/>
                                                    <Setter TargetName="BadgeText" Property="Foreground" Value="#8B2BB0"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Header="Actions" Width="320">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                                <!-- Accept -->
                                                <Button Height="36" Width="Auto" Click="AcceptRow_Click" Foreground="White" FontWeight="SemiBold" Margin="0,0,6,0" Style="{StaticResource RoundedGreenButtonStyle}">
                                                    <Button.ContentTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                <svg:SvgViewbox Source="/Assets/Images/ready.svg" Width="14" Height="14" Margin="8" VerticalAlignment="Center"/>
                                                                <TextBlock Text="Accept" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </Button.ContentTemplate>
                                                </Button>

                                                <!-- Reject -->
                                                <Button Height="36" Width="Auto" Click="RejectRow_Click" Foreground="White"  FontWeight="SemiBold" Margin="0,0,6,0" Style="{StaticResource RoundedRedButtonStyle}">
                                                    <Button.ContentTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                <svg:SvgViewbox Source="/Assets/Images/circle_cross.svg" Width="14" Height="14" Margin="8" VerticalAlignment="Center"/>
                                                                <TextBlock Text="Reject" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </Button.ContentTemplate>
                                                </Button>

                                                <!-- View details (existing) -->
                                                <Button Height="36" Click="OpenOrderDetailPoPUp_Click" Width="130"
                                                    Style="{StaticResource RoundedWhiteButtonStyle}" BorderBrush="#dedede">
                                                    <Button.ContentTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                                <svg:SvgViewbox Source="/Assets/Images/eye.svg" Width="14" Height="14" Margin="8" VerticalAlignment="Center"/>
                                                                <TextBlock Text="Veiw Details" FontSize="14" Foreground="#030213" FontWeight="SemiBold" VerticalAlignment="Center" Margin="8"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </Button.ContentTemplate>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                            </GridView>
                        </ListView.View>
                        <ListView.Resources>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="Height" Value="48"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GridViewColumnHeader">
                                            <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.Resources>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>